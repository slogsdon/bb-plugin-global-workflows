dist/
node_modules/

# Generated by `bb plugin types` from the running bb. These are bb
# workspace contracts, not ours to redistribute, and they go stale against
# a newer bb. Run `bb plugin types` after cloning.
types/
